{% extends 'base.html.twig' %}

{% block body %}


    <div class="row main">
        <div class="panel-heading">
            <div class="panel-title text-center">
                <h1 class="title">Student edit</h1>
                <hr />
            </div>
        </div>
    </div>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
    <button type="submit" class="btn btn-success" value="Edit" >Edit</button>
    {{ form_end(edit_form) }}


            <a type="button" class="btn btn-warning" href="{{ path('student_index') }}">Back to the list</a>

            {{ form_start(delete_form) }}
    <button type="submit" class="btn btn btn-danger" value="Delete">Delete</button>
            {{ form_end(delete_form) }}

{% endblock %}
